# 大数据学期项目计划

⼤数据学期项⽬计划
在过去的14周⾥，我们⼀直在孤⽴地构建各个组件。你们编写了Spark任务，启动了Kafka代理，查询了⽂
档存储，并将⽂本嵌⼊到向量空间中。
在现实世界中——⽆论你是加⼊⼤型科技公司的基础设施团队，还是A轮创业公司——没有⼈会为那些"在我
的笔记本上能运⾏"的孤⽴脚本付费。⾏业为可靠、⾃动化、可扩展的系统付费。
在你们的毕业设计项⽬中，你们将构建这样⼀个系统。
以下是课程最后阶段的参与规则、可交付成果和评分机制。请仔细阅读。将这份⽂件视为你们的第⼀份⼯程
需求规格说明书。
1. 任务：选择你的技术⽅向
正如课堂上所讨论的，你们将组成团队，并从三个架构⽅向中选择⼀个。虽然业务领域不同，但期望达到的
技术严谨性是相同的。
你们的⽬标： 设计架构、摄取数据、处理数据并提供服务。
2. 可交付成果
你们的团队将主要根据两个主要可交付成果进⾏评估。没有传统的"期末考试"——这个项⽬就是你们能⼒的
证明。
A. 系统与报告论⽂（⼯程设计⽂档）
你们将提交⼀份正式的技术报告。不要像写学术论⽂那样写；要像写科技公司的设计⽂档那样写。它必须包
括：
B. 最终演⽰（"技术总监汇报"）
在最后⼀周，你们的团队将向全班进⾏演⽰。
⽅向 A： 实时欺诈与零售引擎（流处理、Kafka、低延迟）
⽅向 B： ⾦融科技哨兵（数据治理、湖仓⼀体分层架构、Delta Lake）
⽅向 C： 企业⼈⼯智能记忆 / 检索增强⽣成流⽔线（⾮结构化⽂本、向量数据库、⼤型语⾔模型）
执⾏摘要： 这个数据流⽔线解决什么业务问题？
架构图： 清晰的数据流视觉图（数据摄取 -> 处理 -> 存储 -> 服务）。
技术权衡： 为什么选择Spark⽽不是Pandas？为什么选择Mongo⽽不是Postgres？为你的选择辩护。
云财务与成本估算： 如果将此系统部署在阿⾥云/华为云上以处理每天10TB的数据，⼤概需要多少成
本？（计算、存储、API调⽤）。
事后剖析： 什么出了问题？已知的bug有哪些？（在此诚实可得⾼分；隐藏bug会扣分）。
代码库： ⼀个Jupter Notebook，准确解释如何运⾏你们的流⽔线。
形式： 最多10分钟，随后5分钟问答。
演⽰： 向我们展⽰系统运⾏。（专业提⽰：最好提前录制好你们⼯作流⽔线的视频。现场演⽰依赖于"演
⽰之神"，⽽众所周知他们⾮常残酷）。

---

3. 评分机制（团队 vs. 个⼈）
构建数据系统是⼀项团队运动。在我⾃⼰的公司⾥，⼀个拒绝协作、拖累团队⼠⽓的杰出⼯程师，对组织⽽
⾔是净負值。因此，你们的分数将同时反映系统质量和你们的专业贡献。
第⼀步：团队基础分（满分100分）
我将根据报告（60%）和演⽰/展⽰（40%）来评估整个项⽬。假设你们的团队构建了⼀个很棒的系统，得
到了 90/100 分。
第⼆步：个⼈乘数（同⾏评议）
项⽬结束时，每个学⽣将提交⼀份保密的同⾏评议表，按照评分标准（例如，编码、沟通、可靠性）评价他
们的队友。
场景举例：
*注意：如果不属实，你不能通过给每个⼈打满分来钻空⼦。如果不能说明每段代码是谁写的，我会将⼩组
中每个⼈的个⼈乘数都记为0.6。
4. ⽣存指南：项⽬开始前与进⾏中
开始之前：
冲刺期间：
结语
答辩： 我将扮演你们的技术总监。我会问你们，如果⼀个节点发⽣故障、数据延迟到达、或者API限
流，会发⽣什么。准备好为你们的架构辩护。
我将使⽤这些评价来计算⼀个个⼈乘数（范围从0.5到1.15）。
计算公式： 个人分数 = 团队基础分 × 个人乘数
可靠成员（乘数 = 1.0）： 你恰好完成了⾃⼰应承担的那份⼯作。你的分数是 90 × 1.0 = 90 。
幽灵成员（乘数 = 0.7）： 你缺席会议，零代码提交，让你的团队陷⼊恐慌。你的分数是 90 × 0.7 =
63 。
技术带头⼈（乘数 = 1.1）： 你超越期望，调试队友的代码并协调最终的代码合并。你的分数是 90 ×
1.1 = 99 。
组建平衡的团队。 不要把四个机器学习建模⼈员放在⼀个团队⾥；你们需要有⼈愿意编写那些不引⼈注
⽬但必要的数据管道⼯作（Docker、Airflow、Spark）。
积极控制范围。 ⼀个能完美端到端运⾏的简单系统就是A。⼀个庞⼤、复杂但在第⼆步就崩溃的系统是
C。
警惕"集成地狱"。 不要等到第16周才把你的Python爬⾍连接到你的Kafka主题，再连接到你的Spark任
务。⾸先构建⼀个"⾏⾛的⻣架"——在第三天就让⼀⾏模拟数据通过整个流⽔线。然后，再让它变得复
杂。
⼈⼯智能辅助⼯具的使⽤： ⿎励你们使⽤⼤型语⾔模型来⽣成样板代码或调试语法错误——这就是现代
⼯程的⼯作⽅式。然⽽，你们对代码负有严格责任。如果我在你们演⽰时指着⼀块代码，问它如何处理
内存分区，⽽你们的回答是"这是⼤模型写的，我不知道"，你们的答辩将不及格。

---

这个项⽬旨在成为你们作品集的核⼼部分。当你们在顶级科技公司⾯试，被问到"请谈谈你处理复杂数据的
⼀次经历"时，这就是你们将要讲述的故事。
拥抱挑战。构建⼀个你们引以为豪的作品。
如果遇到架构上的障碍，我的办公时间随时开放。让我们开始⼯作吧。
附录 A：毕业设计⽅向规范
为确保评分的公平性，每个团队必须构建⼀个包含数据摄取、处理、存储和服务的系统。然⽽，该系统的形
态取决于你们选择的⽅向。
请仔细阅读以下规范。如果你们的架构不满⾜所选⽅向的"必需架构"约束，你们的系统将被视为不完整。
⽅向 A：实时欺诈与⻛险监控平台
核⼼挑战： 毫秒级决策与不可变历史存储相结合。
场景：
你们正在为⼀家⾦融科技独⻆兽公司或⼀个⼤型电⼦商务平台构建⻛险引擎。全球范围内每分钟发⽣数百万
笔交易。你们必须在交易发⽣时检测欺诈⾏为（流处理），通知运营团队，并为每笔交易存储⼀个永久的、
符合法律规定的记录（批处理/湖仓⼀体），供数据科学团队训练未来模型使⽤。
必需架构（基本框架）：
你们的"空⽩画布"（创意⾃由）：
什么能得'A'： 我可以运⾏你们的Python Kafka代码，有意注⼊⼀笔"坏"交易，并在⼏秒钟内看到它出现在你
们的告警数据库中，同时验证原始数据安全地落⼊了你们的Delta Lake。
⽅向 B：智能客⼾⽀持与检索增强⽣成助⼿
核⼼挑战： ⾮结构化数据流⽔线、语义搜索和⼤型语⾔模型接地。
1. 数据摄取（事件流）： 你们必须编写⼀个Python代码，持续⽣成模拟的JSON格式交易数据（⽤⼾ID、
⾦额、时间戳、地点、商⼾等），并将其推送到Kafka主题中。
2. 处理（流引擎）： 使⽤Spark结构化流消费Kafka主题。你们必须⾄少实现⼀个有状态操作（例如，⼀个
滚动/滑动窗⼝，计算"过去5分钟内每个⽤⼾的交易次数"，以检测速度欺诈）。
3. 存储（湖仓⼀体）：
将所有原始事件写⼊原始层Delta表。
将掩码版本（对⽤⼾的个⼈⾝份信息/电⼦邮件进⾏加密哈希处理）写⼊清洗层Delta表。
4. 服务（告警接收端）： 当Spark根据你们的规则检测到欺诈事件时，它必须将该特定事件写⼊⼀个操作型
数据库（例如，MongoDB 或 PostgreSQL）或⼀个实时控制台/仪表板，供欺诈团队审查。
领域： 这是信⽤卡欺诈？加密货币洗售？电⼦商务账⼾被盗？你们决定。
逻辑： 你们定义欺诈启发式规则。⼀个⽤⼾的IP在10分钟内从纽约跳到东京是否会触发警报？凌晨3:00
的⼀笔5,000美元购买是否会触发警报？

---

场景：
你们的企业拥有⼀个庞⼤的内部存储库，⾥⾯充满了混乱的⾮结构化数据——过去的客⼾⽀持⼯单、PDF⼿
册和Slack⽇志。员⼯们花费数⼩时寻找答案。你们的任务是为⼀个检索增强⽣成系统构建数据流⽔线。你
们不仅仅是在构建⼀个ChatGPT包装器；你们是在构建⼀个稳健的数据⼯程后端，为⼈⼯智能提供数据。
必需架构（基本框架）：
你们的"空⽩画布"（创意⾃由）：
什么能得'A'： 你们能够展⽰⼀个⼲净、⾃动化的流⽔线，它接受⼀个包含杂乱⽂本⽂件的原始⽂件夹，并
将其转变为⼀个可搜索的向量索引。当被查询时，你们的系统能够检索到确切正确的段落，并且⼤型语⾔模
型能准确引⽤它。
⽅向 C：城市交通与智慧城市数据平台
核⼼挑战： 湖仓⼀体分层架构、复杂连接和运营分析。
场景：
你们是城市交通部⻔（或像滴滴这样的公司）的⾸席数据⼯程师。你们拥有⼤量历史数据（站点位置、历史
出⾏模式）以及连续不断的实时遥测数据（⾃⾏⻋被停靠、公交⻋移动、乘⻋请求）。你们必须将这两种模
式统⼀到⼀个单⼀的"湖仓⼀体"平台中，以同时⽀持实时运营和⻓期城市规划。
必需架构（基本框架）：
1. 数据摄取（批处理/⽂档）： 摄取⼀个⼤型⽂本数据集（例如，Kaggle上的亚⻢逊评论数据集、ArXiv研
究论⽂或软件⽂档）。
2. 处理（清洗与分块）： 使⽤PySpark或Python清洗⽂本（移除HTML标签，处理错误编码）。关键的是，
你们必须编写逻辑将⽂本分割成语义上有意义的"块"，并提取元数据（作者、⽇期、类别）。
3. 嵌⼊（向量化）： 将你们的⽂本块通过嵌⼊模型（例如，HuggingFace的 all-MiniLM-L6-v2 或OpenAI
API）进⾏处理，将⽂本转换为密集向量。
4. 存储与服务（向量数据库）： 将向量和元数据加载到向量数据库（例如，ChromaDB、Milvus 或
Qdrant）。
5. 查询应⽤程序： 编写⼀个Python脚本，⽤⼾输⼊⾃然语⾔问题。系统必须执⾏混合搜索（语义向量搜索
+ SQL⻛格的元数据过滤，例如，"查找关于'登录错误'的⽂档，但仅限2023年的"）。将检索到的上下⽂
传递给⼤型语⾔模型以⽣成最终答案。
数据集： 选择⼀个你感兴趣的领域。医学期刊？法律合同？电⼦游戏传说？
分块与搜索策略： 你们如何分块数据以及如何调整检索参数（前K个结果、相似度阈值）完全由你们决
定。
1. 数据摄取（批处理 + API/模拟流）：
批处理： 加载历史参考数据（例如，城市站点位置的CSV⽂件、⼈⼝统计数据或历史天⽓数据）。
增量数据： 编写⼀个脚本，模拟每隔⼏秒钟到达的实时遥测数据（例如，公交⻋的GPS信号，或乘
⻋开始/结束事件）。
2. 处理（统⼀引擎）： 使⽤PySpark。你们必须将快速移动的事件数据与缓慢变化的历史参考数据连接起
来（例如，将实时GPS坐标匹配到静态的区域多边形，或将实时乘⻋事件与历史天⽓条件匹配）。
3. 存储（严格的分层架构）：
⻘铜层： 原始摄取数据（JSON/CSV）。
⽩银层： 清洗、连接后的数据，以 Delta Lake / Parquet 格式存储（时间戳标准化，处理空值）。

---

你们的"空⽩画布"（创意⾃由）：
什么能得'A'： ⼀个完美的分层架构。我希望看到你们的流⽔线能够轻松处理新⼀批原始数据，将其通过⻘
铜层、⽩银层和⻩⾦层进⾏传播，并⽴即在最终的仪表板上反映出更新后的聚合结果。
警告：常⻅失败模式
⽆论选择哪个⽅向，都不要落⼊以下陷阱：
明智地选择你们的⽅向。组建团队。本周就开始构建系统⻣架。
⻩⾦层： 聚合后的业务级表（例如， 每个区域的每日出行次数 、 当前站点利用率 ）。
4. 服务（分析）： 将简单的仪表板⼯具（Streamlit、Dash，甚⾄是带有Matplotlib/Seaborn的Jupyter笔记
本）直接连接到你们的⻩⾦层表，以展⽰洞察结果。
城市/领域： 纽约CitiBike共享单⻋数据、芝加哥犯罪数据、伦敦交通数据，或模拟的Uber动态定价数
据。
分析： 业务价值是什么？你们是在预测哪些⾃⾏⻋站点会在下午5:00前空置？你们是在绘制降⾬与交通
延误之间的相关性吗？
1. "⽤⼾界⾯陷阱"： 我不关⼼你们的仪表板是否有漂亮的CSS或时髦的React前端。这是⼀⻔数据系统课
程。如果你们花20⼩时在⽹络⽤⼾界⾯上，⽽只花2⼩时在Spark流⽔线上，你们将不及格。⼀个能打印
出完美⼯程数据的终端输出，远远优于⼀个建⽴在垃圾数据之上的漂亮⽹络应⽤。
2. "上帝脚本"： 不要把所有代码都放在⼀个巨⼤的 main.py ⽂件或单个Jupyter Notebook中。要将代码模
块化。为数据摄取、处理和服务编写单独的脚本。
3. "硬编码路径"： 如果你们的代码包含 filepath = "C:/Users/John/Desktop/data.csv" ，它会在我的
机器上崩溃，你们将被扣分。使⽤相对路径或环境变量。提供 requirements.txt 或 docker-
compose.yml 。

---

